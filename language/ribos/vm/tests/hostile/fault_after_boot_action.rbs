@policy(
    capabilities=[
        Capability.BOOT,
    ],
    helper_budget=1,
)
def boot(ctx: BootContext) -> Result[BootAction, BootError] {
    let action = boot.recovery(RecoveryReason.REQUESTED)
    return Err(BootError.FAILED)
}
