qstar.subdir("host")

qstar.subdir("uefi")

qstar.subdir("bios")

qstar.subdir("rpi")

qstar.group "ribon_firmware" {
  deps = {
    "//src/firmware/host:ribon_firmware_host",
    "//src/firmware/uefi:ribon_firmware_uefi",
    "//src/firmware/bios:ribon_firmware_bios",
    "//src/firmware/rpi:ribon_firmware_rpi",
  },
}
