# Installed SDK deployment consumer

이 template은 source checkout 없이 설치된 public header, target library, host tool과
schema만 사용해 recovery/update product graph, Ribos policy artifact와 canonical update
manifest reader를 구성한다. 결과는 host build/contract evidence이며 firmware 실행이나
production key custody를 주장하지 않는다.
