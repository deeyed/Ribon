# Ribon external plugin package template

Package 작성자는 `package.json`, `plugin.qst`, public header, source, contract test와
documentation을 함께 제공한다. Plugin descriptor는 설치된 `include/Ribon/`만 참조하고
generated registry 또는 source-private header를 수정하지 않는다.
